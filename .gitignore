# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
venv/
.venv/

# macOS
.DS_Store

# IDE / AI tooling
.vscode/
.idea/
.claude/

# Training logs
*.log

# Project - don't track large image/archive files
good-examples/
raw-scans/
output/
errors/
diagnose_out/
random-samples/

# Model checkpoints (large binary files — not tracked in git)
*.pt

# Ground truth backups (the live data/ground_truth.json IS tracked — it's the
# hand-labeled dataset and must be backed up in git)
*.bak

# Virtual environments
venv-cs/
